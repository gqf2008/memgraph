[package]
name = "mgdisk"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
mgcore = { path = "../mgcore" }
mgslk = { path = "../mgslk" }
bincode = "1.3"
fxhash = "0.2"
serde = { workspace = true }
# RocksDB via FFI (rust-rocksdb wraps C++ RocksDB):
#   https://crates.io/crates/rocksdb
# For pure-Rust disk mode, see mgstorage in-memory + WAL/Snapshot durability.

